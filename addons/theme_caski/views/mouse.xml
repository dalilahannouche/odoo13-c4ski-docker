<odoo>

<template id="mouse_caski" name="Mouse" inherit_id="web.layout">
        <xpath expr="//body" position="after">         
          
        <div id="cursor">
        <div class="cursor--inner"></div>
        </div>
        <script type="text/javascript">
const cursor = document.querySelector('#cursor');
let mouse = { x: 300, y: 300 };
let pos = { x: 0, y: 0 };
const speed = 0.1; // between 0 and 1

const updatePosition = () => {
  pos.x += (mouse.x - pos.x) * speed;
  pos.y += (mouse.y - pos.y) * speed;
  cursor.style.transform = 'translate3d(' + pos.x + 'px ,' + pos.y + 'px, 0)';
};

const updateCoordinates = e => {
  mouse.x = e.clientX;
  mouse.y = e.clientY;
}

window.addEventListener('mousemove', updateCoordinates);

function loop() {
  updatePosition();
  requestAnimationFrame(loop);
}

requestAnimationFrame(loop);

        </script>
        </xpath>
      </template>


    <template id="assets_frontendmouse" name="mouse assets" inherit_id="theme_caski.mouse_caski">
        <xpath expr="." position="inside">
         	<link rel="stylesheet" type="text/css" href="/theme_caski/static/src/css/mouse.css"/>
        </xpath>
    </template>


</odoo>
